# Build output and dependencies
node_modules/
dist/
*.log

# On-chain state generated at runtime (not source)
.treasury-onchain/

# SECRETS — never commit these.
.env
.env.*
!.env.example
*.skey
*.vkey
*.key
*.pem
*.pat
*.mnemonic
*.seed

# Assistant rules and state — belong to an individual machine, not to this repo
_Agents/
CLAUDE.md
.claude/
_wt-*

# LAMP pinned to a commit, materialised by scripts/pin-lamp.sh — never commit another repo's code here.
vendor/lamp/
